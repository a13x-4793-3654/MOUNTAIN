-- =====================================================================
-- MOUNTAIN 本番(prod)用 初期データ ＝ 空
-- ---------------------------------------------------------------------
-- 本番はここでは業務データも土台データも投入しない（＝空のまま起動）。
-- データベースの初回作成時は 01_schema.sql（テーブル定義）だけが実行され、
-- 業務の土台（区分マスタ・権限・着信アナウンス定義・システム利用者2件）は、
-- アプリが最初にWebアクセスされたときに自分で自動生成する
--   （app/bootstrap.py の ensure_baseline() が app/baseline.sql を冪等に適用）。
--
-- これにより本番は「空のデータベースを用意して起動するだけ」で、
-- 手作業の初期セットアップなしに、最初のアクセス時点で土台が整う。
--
-- ※ DF（開発・検証）は従来どおり db/seed.sql（デモデータ）を使う。
--    この空シードは本番配置ツール(_deploy_prod.py)が db/seed.sql として転送する。
-- =====================================================================
SELECT 1;
